<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aotome202.lostjason.x103aotom">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".application.MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="design_width" android:value="768">
        </meta-data>
        <meta-data android:name="design_height" android:value="1280">
        </meta-data>
        <activity android:name=".activity.BootActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.About_Settting"/>
        <activity android:name=".activity.Device_Settting"/>
        <activity android:name=".activity.Luxiang_Settting"/>
        <activity android:name=".activity.Mirror_Equipment"/>
        <activity android:name=".activity.RecorderTime"/>
        <activity android:name=".activity.Equipment_Settting"/>

    </application>

</manifest>